<title> Product Listing </title>

  <h1>Your Products</h1>
  <table>
    <tr>
      <th>Name</th>
      <th>Overview</th>
    </tr>

  <% @products.each do |product| %>
    <tr>
      <td><%= link_to product.name, product %></td>
      <td><%= product.overview %></td>
    </tr>
  <% end %>
  </table>

  <br />
<%= button_to 'Add a New Product', {:controller => "products", :action => "new"}, :method => :get, :class => 'add_button' %>